pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'companies-bi-processing'

// Sub-modules
include 'modules:processing'
