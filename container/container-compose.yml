version: '3.0'
services:
  postgresql:
    image: 'postgres:13'
    ports:
      - '5432:5432'
    environment:
      - 'POSTGRES_PASSWORD=postgres'
    volumes:
      - './docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d:ro'
