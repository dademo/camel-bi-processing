spring:
  application:
    name: Supervision task runner
  liquibase:
    enabled: false

task:
  datasource:
    url: jdbc:postgresql://localhost:5432/bi
    username: postgres
    password: postgres

module:
  datasource:
    url: jdbc:postgresql://localhost:5432/bi
    username: postgres
    password: postgres

logging:
  level:
    root: INFO
    fr.dademo: DEBUG
